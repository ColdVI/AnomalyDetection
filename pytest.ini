[pytest]
testpaths = tests
addopts = -ra
markers =
    e2e: gercek tarayici + calisan Docker stack'i (dashboard-app, localhost:8050) gerektirir -- diger 420 test hermetiktir, bunlar DEGIL. Hizli/varsayilan calistirmadan haric tutmak icin: pytest -m "not e2e"
