# Proje genelinde TEK requirements dosyasi -- src/ pipeline, Dashboard/ ve
# adsb/ ML modelleri hepsi bunu kullanir (Dashboard/requirements.txt ayri
# bir dosyaydi, buraya birlestirildi -- 2026-07-15).
#
# Surumler bu makinede fiilen kurulu/dogrulanmis olanlardir (pip freeze ile
# kontrol edildi). torch sadece adsb/models/ icin gerekli ve bu makinede
# kurulu degil (gercek ADS-B egitimi ayri bir makinede yapiliyor) -- surumu
# dogrulanamadi, mevcut pin korundu.
#
# pydeck, openpyxl, pyulog, rosbags, lightgbm, chronos-forecasting kaldirildi:
# aktif kodda (src/, Dashboard/, adsb/) hicbir yerde import edilmiyorlar,
# sadece archive/2026-07-10_legacy_non_adsb_ml/ altindaki dondurulmus eski
# ML kodu kullaniyordu (bkz. archive/README.md -- "Arsiv aktif dependency
# degildir"). O arsivi calistirmak gerekirse ayrica kurulmalilar.

confluent-kafka==2.15.0
redis==8.0.1
influxdb-client==1.50.0
minio==7.2.20
pandas==3.0.3
numpy==2.5.0
pyarrow==24.0.0
scikit-learn==1.9.0
fastapi==0.138.2
uvicorn[standard]==0.49.0
dash==4.3.0
dash-leaflet==1.1.3
dash-extensions==2.0.5
plotly==6.8.0
requests==2.34.2
python-dotenv==1.2.2
pytest==9.1.1
scipy==1.18.0
joblib==1.5.3

# adsb/models/ icin -- bu makinede kurulu degil, surum dogrulanamadi
torch==2.12.1
